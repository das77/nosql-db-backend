# Test-only values. Not a secret: this JWT_SECRET signs short-lived tokens against a
# throwaway in-memory MongoDB that exists only for the duration of a test run.
# Committed deliberately so `npm test` works on a fresh clone with no local .env.
JWT_SECRET=test-secret-not-used-outside-jest
